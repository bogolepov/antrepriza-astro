---
import theater from '@data/theater.json'
import dictionary from '@data/dictionary.json'

export function getStaticPaths () {
  return theater.languages.map(lng => {
    return {
      params: {lang: lng.lang_code},
      props: {pageLang: lng.lang_code, pageTitle: theater.shortTheaterName[lng.lang_code] + ' - ' + dictionary.privacy_policy[lng.lang_code] },
    };
  });
}

const { pageLang, pageTitle } = Astro.props;

import AntreprizaLayout from "@layouts/AntreprizaLayout.astro";
import PrivacyPolicy from "@components/privacy_policy.astro";
---

<AntreprizaLayout title={pageTitle} lang={pageLang}>
  <PrivacyPolicy lang={pageLang}/>
</AntreprizaLayout>

