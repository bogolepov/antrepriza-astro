---
import theater from '@data/theater.json'
import dictionary from '@data/dictionary.json'

export function getStaticPaths () {
  return theater.languages.map(lng => {
    return {
      params: {lang: lng.lang_code},
      props: {pageLang: lng.lang_code, pageTitle: theater.longTheaterName[lng.lang_code] },
    };
  });
}

const { pageLang, pageTitle } = Astro.props;

import AntreprizaLayout from "../../layouts/AntreprizaLayout.astro";
import PlaysCarusel from "@components/carusel_main.astro"
import AfishaLayout from "@layouts/afisha.astro";
---

<AntreprizaLayout title={pageTitle} lang={pageLang}>
  <div class="wrapper1">
    <PlaysCarusel lang={pageLang}>
  </div>
  <div class="wrapper2">
    <AfishaLayout lang={pageLang}/>
  </div>
</AntreprizaLayout>

<style>
  .wrapper1 {
    margin-bottom: 2rem;
  }

  .wrapper2 {
    margin: 0px clamp(15px, 4.5vw, 50px);
  }
</style>

