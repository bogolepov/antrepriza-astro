---
import type { GetStaticPaths } from "astro";

import AntreprizaLayout from "@layouts/AntreprizaLayout.astro";
import Person from "@components/person.astro";

// import plays from '@data/plays.json'
import dictionary from '@data/dictionary.json'
import theater from '@data/theater.json'

export const getStaticPaths = (() => {
  let team = theater.people.filter(person => person.suffix);  
  return theater.languages.flatMap(lng => {
    return team.map(person => {
      let aTitle = theater.shortTheaterName[lng] + " - " + person.name[lng] + " " + person.surname[lng];
      // console.log(aTitle);
      let item =  {
        params: {lang: lng, id_suffix: person.suffix},
        props: {pageLang: lng, pageTitle: aTitle, personID: person.id.toString()},
      }
      // console.log(item);
      
      return item;
    })
  });
}) satisfies GetStaticPaths;

const {pageLang, pageTitle, personID} = Astro.props;

---

<AntreprizaLayout title={pageTitle} lang={pageLang}>
    <Person lang={pageLang} person_id={personID}></Person>
</AntreprizaLayout>



