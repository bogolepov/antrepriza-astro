import { ONE_HOUR, ONE_MONTH, ONE_YEAR } from '@scripts/consts';

export const ACCESS_TOKEN_TIME = ONE_HOUR / 1000; // in seconds
// export const ACCESS_TOKEN_TIME = 40; // in seconds
export const REFRESH_TOKEN_TIME = (3 * ONE_MONTH) / 1000; // in seconds
