import { ONE_HOUR, ONE_YEAR } from '@scripts/consts';

// export const ACCESS_TOKEN_TIME = ONE_HOUR / 1000; // in seconds
export const ACCESS_TOKEN_TIME = 60; // in seconds
export const REFRESH_TOKEN_TIME = ONE_YEAR / 1000; // in seconds
